<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iti.gov.mashawery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mashawery">
        <activity android:name=".history.view.HistoryActivity"></activity>
        <activity android:name=".trip.view.TimePickers" />
        <activity android:name=".registeration.view.LoginActivity" />
        <activity android:name=".registeration.view.RegisterActivity" />
        <activity android:name=".trip.view.AddTripActivity" />
        <activity android:name=".home.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>