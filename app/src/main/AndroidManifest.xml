<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iti.gov.mashawery">

    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACTION_MANAGE_OVERLAY_PERMISSION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mashawery">
        <activity android:name=".reminder.view.ReminderActivity"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog.Alert"></activity>
        <activity android:name=".trip.edit.view.EditTripActivity" />
        <activity android:name=".history.view.HistoryActivity" />
        <activity android:name=".trip.create.view.TimePickers" />
        <activity android:name=".registeration.view.LoginActivity" />
        <activity android:name=".registeration.view.RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trip.create.view.AddTripActivity"></activity>
        <activity android:name=".home.view.MaineActivity"></activity>
        <activity android:name=".home.view.MainActivity"></activity>
        <receiver
            android:name=".reminder.view.TripReminderReciever"
            android:directBootAware="true"
            android:process=":remote"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>